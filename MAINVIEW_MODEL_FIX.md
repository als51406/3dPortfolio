# 🔧 메인뷰 3D 모델 로딩 문제 해결 가이드

## 🐛 증상
- 메인뷰에서 시계 모델이 사라짐
- 콘솔 에러: `Could not load /models/apple_watch_ultra_2.glb: Unexpected token '<', "<!DOCTYPE "`

## 🔍 원인
브라우저 캐시 문제로 인해 모델 파일 대신 HTML 페이지가 로드됨

## ✅ 해결 방법

### 1단계: 브라우저 캐시 완전히 클리어

#### Chrome/Edge
1. **개발자 도구 열기**: `F12` 또는 `Cmd+Option+I` (Mac)
2. **Application 탭** 클릭
3. 좌측 **Storage** 메뉴 확장
4. **Clear site data** 버튼 클릭
5. 모든 항목 체크 후 **Clear site data** 실행

#### 또는 간단한 방법
1. **하드 리프레시**: `Cmd+Shift+R` (Mac) 또는 `Ctrl+Shift+R` (Windows)
2. 안 되면 **시크릿 모드**에서 테스트: `Cmd+Shift+N` (Mac) 또는 `Ctrl+Shift+N` (Windows)

---

### 2단계: 개발 서버 재시작 (이미 완료)

```bash
# 포트 정리
lsof -ti:3000 | xargs kill -9

# 서버 재시작
npm start
```

---

### 3단계: 코드 확인 (이미 수정 완료)

#### ✅ MyElement3D.tsx
```tsx
// 모델 사전 로드 추가
useGLTF.preload("/models/apple_watch_ultra_2.glb");

function MyElement3D() {
    const model1 = useGLTF("/models/apple_watch_ultra_2.glb");
    // ...
}
```

#### ✅ Detailview.tsx
```tsx
const MODEL_URL = "/models/apple_watch_ultra_2.glb";
useGLTF.preload(MODEL_URL as string);
```

---

## 🎯 메인뷰와 디테일뷰 모델 관리

### 현재 구조
```
메인뷰 (Mainview.tsx)
  └─ MyElement3D.tsx
       └─ useGLTF("/models/apple_watch_ultra_2.glb")
            └─ 8개의 시계를 원형으로 배치

디테일뷰 (Detailview.tsx)
  └─ ProductModel 컴포넌트
       └─ useGLTF("/models/apple_watch_ultra_2.glb")
            └─ 1개의 시계 + 360도 회전 + 줌인/아웃
```

### 특징
- ✅ **동일한 모델 파일 사용**: `/models/apple_watch_ultra_2.glb`
- ✅ **독립적인 컴포넌트**: 각각의 useGLTF 훅으로 별도 관리
- ✅ **scene.clone()**: 메모리 효율적으로 복제하여 사용
- ✅ **preload**: 각 컴포넌트에서 사전 로드로 성능 최적화

---

## 🧪 테스트 체크리스트

### 메인뷰 확인
- [ ] 페이지 로드 시 8개의 시계가 원형으로 배치되어 있는가?
- [ ] 중앙에 분홍색 라이트가 회전하고 있는가?
- [ ] 스크롤 시 카메라가 부드럽게 움직이는가?

### 디테일뷰 확인
- [ ] 스크롤 다운 시 시계가 부드럽게 페이드인되는가?
- [ ] 마우스 드래그로 360도 회전이 가능한가?
- [ ] 텍스트가 순차적으로 나타나는가?

### 콘솔 확인
- [ ] 빨간 에러가 없는가?
- [ ] GLB 로딩 에러가 없는가?
- [ ] WebSocket 연결 정상인가?

---

## 🚨 여전히 문제가 있다면?

### 1. public 폴더 확인
```bash
ls -lh public/models/apple_watch_ultra_2.glb
# 출력 예시: -rw-rw-r-- 15M apple_watch_ultra_2.glb
```

### 2. 브라우저 네트워크 탭 확인
1. F12 → **Network** 탭
2. 페이지 새로고침
3. `apple_watch_ultra_2.glb` 검색
4. **Status**가 `200 OK`인지 확인
5. **Type**이 `application/octet-stream`인지 확인

### 3. 다른 브라우저 테스트
- Safari에서 테스트
- Firefox에서 테스트

### 4. 포트 변경
```bash
# .env 파일 생성
echo "PORT=3001" > .env

# 서버 재시작
npm start
```

---

## 📊 모델 파일 정보

```
파일명: apple_watch_ultra_2.glb
크기: 15MB
위치: /public/models/
포맷: GLB (Binary glTF)
사용처: 
  - MyElement3D.tsx (메인뷰, 8개 복제)
  - Detailview.tsx (디테일뷰, 1개 + 애니메이션)
```

---

## 💡 추가 최적화 팁

### 1. 모델 파일 압축
```bash
# gltf-pipeline 설치 (선택사항)
npm install -g gltf-pipeline

# 모델 압축 (파일 크기 감소)
gltf-pipeline -i apple_watch_ultra_2.glb -o apple_watch_ultra_2_compressed.glb -d
```

### 2. CDN 사용 (배포 시)
```tsx
const MODEL_URL = process.env.NODE_ENV === 'production'
  ? "https://cdn.example.com/models/apple_watch_ultra_2.glb"
  : "/models/apple_watch_ultra_2.glb";
```

### 3. Loading State 추가
```tsx
import { Suspense } from 'react';

<Suspense fallback={<LoadingSpinner />}>
  <MyElement3D />
</Suspense>
```

---

## ✅ 최종 확인

개발 서버가 실행 중이고 (`npm start`), 브라우저 캐시를 클리어했다면:

1. http://localhost:3000 접속
2. `Cmd+Shift+R` (하드 리프레시)
3. F12 콘솔에서 에러 확인
4. 메인뷰에 시계 8개가 보이는지 확인

**모든 것이 정상이라면 메인뷰의 시계들이 아름답게 회전하고 있을 것입니다!** ✨

---

## 📞 여전히 문제가 있나요?

현재 상태를 알려주세요:
1. 브라우저 콘솔의 에러 메시지
2. 네트워크 탭의 apple_watch_ultra_2.glb 상태
3. 어떤 브라우저를 사용하고 있는지

이 정보로 추가 디버깅을 도와드리겠습니다! 🚀
